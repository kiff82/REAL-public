"""
REAL helper package.

This package is intentionally small and "pure": it provides canonical helper
functions used across scripts so semantics (prefix concatenation, loss
computation, protocol metadata) don't silently drift.
"""
